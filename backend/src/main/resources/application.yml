# AI赋能教育管理与学习辅助平台 - 配置文件
server:
  port: 8080
  servlet:
    context-path: /
    session:
      cookie:
        name: JSESSIONID
        http-only: true
        secure: false
        path: /
        max-age: 86400
      timeout: 86400s

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/education_platform?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: 641641773
    driver-class-name: com.mysql.cj.jdbc.Driver
  main:
    allow-circular-references: true
  # 配置文件上传大小限制
  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB
      enabled: true

# Dify AI平台配置
education:
  dify:
    # Dify服务器地址
    api-url: http://219.216.65.108/v1
    # API密钥配置
    api-keys:
      # 组卷工作流API密钥 - 需要替换为真实的API密钥
      paper-generation: app-D5isfWHTIwVk8t82S507Rkfs
      # 自动批改工作流API密钥 - 需要替换为真实的API密钥 
      auto-grading: app-D5isfWHTIwVk8t82S507Rkfs
      # 知识图谱生成API密钥 - 需要替换为真实的API密钥
      knowledge-graph: app-D5isfWHTIwVk8t82S507Rkfs
    # 请求配置
    timeout: 30000000  # 请求超时时间(毫秒)，增加到5分钟
    retry-count: 3  # 重试次数
# 文件上传配置
file:
  upload:
    path: ./uploads
  access:
    url:
      prefix: /files

# 视频上传路径配置
video:
  upload:
    path: D:/my_git_code/SmartClass/resource/video
    max-size: 2GB